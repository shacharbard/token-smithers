---
phase: 10
plan: "10-05"
title: "Tier 2a — Imports graph (scripting langs: Python / TS / JS)"
type: execute
wave: 5
depends_on: ["10-04"]
cross_phase_deps:
  - "phase-06: extends TreeSitterASTExtractor (adapters/compression/tree_sitter_ast.py) with imports-only queries"
autonomous: true
effort_override: max
skills_used:
  - python-pytest-patterns
files_modified:
  - src/token_sieve/ast/__init__.py
  - src/token_sieve/ast/imports_graph.py
  - src/token_sieve/ast/queries/imports/python.scm
  - src/token_sieve/ast/queries/imports/typescript.scm
  - src/token_sieve/ast/queries/imports/javascript.scm
  - tests/unit/ast/test_imports_graph_python.py
  - tests/unit/ast/test_imports_graph_typescript.py
  - tests/unit/ast/test_imports_graph_javascript.py
forbidden_commands: []
must_haves:
  truths:
    - "New module src/token_sieve/ast/imports_graph.py builds a lightweight imports-only graph per file, reusing the Phase 6 TreeSitterASTExtractor infrastructure"
    - "Per-language tree-sitter queries live in src/token_sieve/ast/queries/imports/*.scm (~10-30 lines each)"
    - "Python: handles `import x`, `from x import y`, `from .relative import z`, `from ..pkg import w`, ignores conditional/try-guarded imports"
    - "TypeScript: handles `import x from 'y'`, `import {a,b} from 'y'`, `import * as x from 'y'`, dynamic `import('y')` expressions, type-only `import type {T}`"
    - "JavaScript: handles CommonJS `require('y')`, ES `import` forms, dynamic imports"
    - "Graph shape: dict[str, set[str]] where key = source file path, value = set of imported module specifiers (unresolved raw strings at this stage — resolution is Task 4)"
    - "Module specifier resolution resolves relative imports against the source file directory and package imports against a configured project root; unresolved externals become `external:<spec>` sentinels"
    - "Parse failures fail open (empty import set for that file), matching Phase 6 error tolerance"
    - "RED commits separate from GREEN commits per task"
  artifacts:
    - {path: "src/token_sieve/ast/imports_graph.py", provides: "ImportsGraph + builder", contains: "class ImportsGraph"}
    - {path: "src/token_sieve/ast/queries/imports/python.scm", provides: "Python imports tree-sitter query"}
    - {path: "src/token_sieve/ast/queries/imports/typescript.scm", provides: "TypeScript imports tree-sitter query"}
    - {path: "src/token_sieve/ast/queries/imports/javascript.scm", provides: "JavaScript imports tree-sitter query"}
  key_links:
    - {from: "src/token_sieve/ast/imports_graph.py", to: "src/token_sieve/adapters/compression/tree_sitter_ast.py", via: "reuses Phase 6 TreeSitterASTExtractor parser/loader"}
---
<objective>
Land Tier 2a of the cache coherence story: a scripting-language imports graph built on token-sieve's own Phase 6 tree-sitter stack. This wave ships Python + TypeScript + JavaScript because they share parsing conventions and cover the majority of token-sieve's user base. Wave 6 adds Go/Rust/Java plus the transitive BFS invalidation path that consumes this graph. Splitting by language family lets Tier 2a test the module/query plumbing on three languages while keeping the wave scope tight.
</objective>
<context>
@.vbw-planning/phases/10-session-intelligence/10-CONTEXT.md (GA2 §Tier 2)
@src/token_sieve/adapters/compression/tree_sitter_ast.py (Phase 6 TreeSitterASTExtractor — reuse, do not fork)
Existing code:
- src/token_sieve/adapters/compression/tree_sitter_ast.py — Phase 6 extractor with per-language grammar loaders
Memory: project_target_audience.md (zero external deps baseline); project_phase6_decisions.md (tree-sitter, language detection, error tolerance).
Note: a new top-level `src/token_sieve/ast/` package is introduced here. If structure conflicts with existing layout, place imports_graph.py under `src/token_sieve/adapters/compression/imports_graph.py` instead. Either path is acceptable; Dev chooses after inspecting the existing package layout.
</context>
<tasks>

<task type="auto">
  <name>Task 1: Module scaffolding + ImportsGraph data structure (RED→GREEN)</name>
  <files>
    src/token_sieve/ast/__init__.py
    src/token_sieve/ast/imports_graph.py
    tests/unit/ast/test_imports_graph_structure.py
  </files>
  <action>
RED — tests/unit/ast/test_imports_graph_structure.py:
  - test_empty_graph_initializes
  - test_add_edge_creates_forward_entry: add_import("a.py", "b") → graph.imports_of("a.py") == {"b"}
  - test_importers_of_reverse_lookup: add_import("a.py", "b"); importers_of("b") == {"a.py"}
  - test_multiple_imports_accumulate
  - test_graph_merge_combines_disjoint: two graphs merge without overlap
  - test_graph_is_serializable: to_dict / from_dict round-trip
Commit RED.

GREEN — src/token_sieve/ast/imports_graph.py with:
  - @dataclass class ImportsGraph with `forward: dict[str, set[str]]`, `reverse: dict[str, set[str]]`
  - add_import(src, dst), imports_of(src), importers_of(dst), merge(other), to_dict, from_dict
  - Both forward and reverse maintained incrementally for O(1) lookup in both directions
Commit GREEN.
  </action>
  <verify>
pytest tests/unit/ast/test_imports_graph_structure.py -v
  </verify>
  <done>
ImportsGraph data structure is complete and tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Python imports query + builder (RED→GREEN→REFACTOR)</name>
  <files>
    src/token_sieve/ast/queries/imports/python.scm
    src/token_sieve/ast/imports_graph.py
    tests/unit/ast/test_imports_graph_python.py
  </files>
  <action>
RED — tests/unit/ast/test_imports_graph_python.py — build a small fixture tree (tmp_path with seeded .py files):
  - test_absolute_import: `import foo` → edge to "foo"
  - test_from_import: `from foo import bar` → edge to "foo"
  - test_from_import_multiple: `from foo import a, b, c` → single edge to "foo" (imports graph is module-level, not symbol-level)
  - test_relative_import_one_dot: in pkg/a.py, `from . import b` → edge to "pkg.b" (resolved)
  - test_relative_import_two_dots: in pkg/sub/a.py, `from .. import b` → edge to "pkg.b"
  - test_conditional_import_still_captured: `try: import foo\nexcept ImportError: ...` → edge to "foo" (capture, don't filter — optimistic)
  - test_syntax_error_fails_open: malformed .py → returns empty import set, no exception
Commit RED.

GREEN — Create src/token_sieve/ast/queries/imports/python.scm with tree-sitter patterns:
  ```
  (import_statement name: (dotted_name) @import)
  (import_from_statement module_name: (dotted_name) @import)
  (import_from_statement module_name: (relative_import) @import_relative)
  ```
Add build_python_imports(path: str) in imports_graph.py that loads the file via Phase 6 TreeSitterASTExtractor, runs the query, collects captures, resolves relative imports against the source directory, returns set[str].
Add build_from_paths(paths, language_detect_fn) that dispatches per language.
Commit GREEN.

REFACTOR — If the query compilation is expensive, cache per-language query objects.
  </action>
  <verify>
pytest tests/unit/ast/test_imports_graph_python.py -v
pytest tests/unit/ -q
  </verify>
  <done>
Python imports graph extracts absolute, relative, multiple-name, conditional imports; fails open on syntax errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: TypeScript imports query + builder (RED→GREEN)</name>
  <files>
    src/token_sieve/ast/queries/imports/typescript.scm
    src/token_sieve/ast/imports_graph.py
    tests/unit/ast/test_imports_graph_typescript.py
  </files>
  <action>
RED — tests/unit/ast/test_imports_graph_typescript.py:
  - test_default_import: `import x from 'y'` → "y"
  - test_named_import: `import {a, b} from 'y'` → "y"
  - test_namespace_import: `import * as x from 'y'` → "y"
  - test_type_only_import: `import type {T} from 'y'` → "y" (still an edge)
  - test_dynamic_import_expression: `const x = await import('y')` → "y"
  - test_relative_import_resolved: in src/a.ts, `import x from './b'` → resolved to "src/b" (or "src/b.ts" if extension resolution is in scope — if not, store as "./b" relative)
Commit RED.

GREEN — queries/imports/typescript.scm with patterns for import_statement (source @import) and call_expression where function is `import` (dynamic). Add build_typescript_imports to imports_graph.py. Commit GREEN.
  </action>
  <verify>
pytest tests/unit/ast/test_imports_graph_typescript.py -v
  </verify>
  <done>
TS imports captured across 5 forms; dynamic import() handled.
  </done>
</task>

<task type="auto">
  <name>Task 4: JavaScript imports query + builder + specifier resolution helper (RED→GREEN→REFACTOR)</name>
  <files>
    src/token_sieve/ast/queries/imports/javascript.scm
    src/token_sieve/ast/imports_graph.py
    tests/unit/ast/test_imports_graph_javascript.py
    tests/unit/ast/test_specifier_resolution.py
  </files>
  <action>
RED — tests/unit/ast/test_imports_graph_javascript.py:
  - test_es_import
  - test_commonjs_require: `const x = require('y')` → "y"
  - test_dynamic_import
  - test_non_import_require_not_captured: `const x = require.resolve('y')` → NOT captured (only call expressions with callee === "require")

RED — tests/unit/ast/test_specifier_resolution.py:
  - test_resolve_relative: ("src/a.ts", "./b") → "src/b"
  - test_resolve_parent_relative: ("src/sub/a.ts", "../b") → "src/b"
  - test_external_becomes_sentinel: ("src/a.ts", "react") → "external:react"
  - test_absolute_import_resolved_against_project_root: ("src/a.ts", "lib/util", project_root="src") → "src/lib/util"

Commit RED.

GREEN — queries/imports/javascript.scm + JS builder + a shared `resolve_specifier(source_path, spec, project_root)` helper that handles relative/absolute/external sentinel cases uniformly for Python/TS/JS. All three builders call into this helper so resolution is consistent. Commit GREEN.

REFACTOR — Consolidate the three builders into a single `build_imports(path, language)` dispatcher that picks the .scm query and calls resolve_specifier.
  </action>
  <verify>
pytest tests/unit/ast/ -v
pytest tests/unit/ -q
  </verify>
  <done>
JS imports captured including CommonJS; specifier resolver handles all three languages uniformly; external packages become `external:<name>` sentinels.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wave merge + hygiene</name>
  <files></files>
  <action>
pip install -e . && python -c "import token_sieve; print(token_sieve.__file__)" && pytest -q
Smoke test: build an ImportsGraph over the token-sieve repo itself (src/token_sieve/**/*.py) and verify the graph has >0 edges and no parse errors (fail-open still succeeds).
  </action>
  <verify>
pytest -q
python -c "from token_sieve.ast.imports_graph import ImportsGraph; print('ok')"
  </verify>
  <done>
main green; Python/TS/JS imports graph works on seeded fixtures and on token-sieve's own source tree.
  </done>
</task>

</tasks>
<verification>
1. pytest tests/unit/ast/ -v — all three language suites green
2. ImportsGraph data structure contract tests green
3. Specifier resolution green across relative/absolute/external
4. pytest -q — full baseline green
</verification>
<success_criteria>
- ImportsGraph module exists and is well-tested
- Three .scm query files for python/typescript/javascript
- Per-language builders ship with seed-fixture coverage
- Specifier resolution shared across languages
- Fail-open on parse errors (Phase 6 parity)
- Zero new external dependencies (reuses Phase 6 tree-sitter)
- RED commits separate from GREEN commits
</success_criteria>
<output>
10-05-SUMMARY.md
</output>
