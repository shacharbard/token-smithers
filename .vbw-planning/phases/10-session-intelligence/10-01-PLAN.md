---
phase: 10
plan: "10-01"
title: "Hygiene warm-up + compress.py regression guard"
type: execute
wave: 1
depends_on: []
cross_phase_deps:
  - "phase-09: compress.py threading workarounds (_run_async, _run_async_bool) must remain byte-equal through refactor"
autonomous: true
effort_override: balanced
skills_used:
  - python-pytest-patterns
files_modified:
  - tests/unit/adapters/embed/test_model2vec_embedder.py
  - tests/unit/cli/test_stats_command.py
  - src/token_sieve/adapters/compression/file_redirect.py
  - src/token_sieve/adapters/compression/progressive_disclosure.py
  - tests/unit/adapters/compression/test_file_redirect_determinism.py
  - tests/unit/adapters/compression/test_progressive_disclosure_determinism.py
  - tests/regression/test_compress_py_threading_guard.py
forbidden_commands: []
must_haves:
  truths:
    - "Every production code change follows RED→GREEN→REFACTOR with RED commit separated from GREEN commit"
    - "DEVN-05 test_repr and test_stats_prints_formatted_table are green and deterministic"
    - "FileRedirectStrategy.deterministic == True (dict ordering stabilized via sorted keys)"
    - "ProgressiveDisclosureStrategy.deterministic == True (timestamp source stubbed/frozen)"
    - "Phase 9 adapter audit re-run shows 0 non-determinism findings for FileRedirect and ProgressiveDisclosure"
    - "A dedicated compress.py regression guard test file exercises _run_async and _run_async_bool end-to-end, captures observable behavior (return values, exception propagation, thread cleanup), and is green on main before wave 2 begins"
    - "The guard test is written against the CURRENT implementation (pre-refactor) so it acts as the behavioral contract for the refactor wave"
    - "All 1627+ baseline tests remain green after wave merge"
    - "Post-merge hygiene: pip install -e . from project root, verify import path resolves to working tree, full pytest green"
  artifacts:
    - {path: "tests/regression/test_compress_py_threading_guard.py", provides: "Regression guard for compress.py _run_async/_run_async_bool", contains: "class TestCompressPyThreadingGuard"}
    - {path: "tests/unit/adapters/compression/test_file_redirect_determinism.py", provides: "Determinism test for FileRedirectStrategy", contains: "test_dict_ordering_stable"}
    - {path: "tests/unit/adapters/compression/test_progressive_disclosure_determinism.py", provides: "Determinism test for ProgressiveDisclosure", contains: "test_timestamp_deterministic"}
  key_links:
    - {from: "tests/regression/test_compress_py_threading_guard.py", to: "src/token_sieve/cli/compress.py", via: "exercises _run_async and _run_async_bool code paths end-to-end"}
---
<objective>
Wave 1 (hygiene warm-up). Land four Phase 9 residue fixes AND a dedicated compress.py behavioral regression guard in a single worktree, so that wave 2 (the compress.py refactor) begins on a green main with a pre-existing contract test in place. The user explicitly moved the regression guard into this wave, so the refactor wave no longer owns the guard as a gating task.
</objective>
<context>
@.vbw-planning/phases/10-session-intelligence/10-CONTEXT.md
@.vbw-planning/phases/10-session-intelligence/10-RESEARCH.md
Relevant source (use jCodeMunch get_symbol_source, NOT Read):
- src/token_sieve/cli/compress.py — functions `_run_async`, `_run_async_bool`, and any call sites
- src/token_sieve/adapters/compression/file_redirect.py — FileRedirectStrategy, marked deterministic=False per Phase 9 wave 5 audit
- src/token_sieve/adapters/compression/progressive_disclosure.py — ProgressiveDisclosureStrategy, marked deterministic=False (timestamp source)
- src/token_sieve/adapters/embed/model2vec_embedder.py — DEVN-05 test_repr failure
- src/token_sieve/cli/stats_shadow.py / report_formatter.py — DEVN-05 test_stats_prints_formatted_table failure
Worktree note: per project_phase09_gotchas.md, worktree isolation does NOT copy gitignored .vbw-planning/. Dev spawn must either copy the planning directory into the worktree or run non-isolated against the main tree.
</context>
<tasks>

<task type="auto">
  <name>Task 1: Fix DEVN-05 test failures (RED→GREEN)</name>
  <files>
    tests/unit/adapters/embed/test_model2vec_embedder.py
    tests/unit/cli/test_stats_command.py
    src/token_sieve/adapters/embed/model2vec_embedder.py (or related)
    src/token_sieve/cli/report_formatter.py (or related)
  </files>
  <action>
RED — Run the two failing tests in isolation, confirm each fails for a real reason (assertion mismatch, not import/setup). Capture the exact failure string for the commit message.
  pytest tests/unit/adapters/embed/test_model2vec_embedder.py::TestModel2VecEmbedder::test_repr -v
  pytest tests/unit/cli/test_stats_command.py::TestStatsCommand::test_stats_prints_formatted_table -v
If a test assertion no longer matches the production code's current output and the production output is correct, update the test (this is a test-drift fix, not a production bug — commit as `test(10-01): realign test_repr/test_stats expectations with current output`). If the production code is the bug, write the failing test assertion first (already failing), commit RED, then fix production, commit GREEN.
  </action>
  <verify>
pytest tests/unit/adapters/embed/test_model2vec_embedder.py tests/unit/cli/test_stats_command.py -v
  </verify>
  <done>
Both DEVN-05 tests green. Commit history shows either a single test-drift commit or separate RED/GREEN commits.
  </done>
</task>

<task type="auto">
  <name>Task 2: FileRedirectStrategy dict ordering determinism (RED→GREEN→REFACTOR)</name>
  <files>
    tests/unit/adapters/compression/test_file_redirect_determinism.py
    src/token_sieve/adapters/compression/file_redirect.py
  </files>
  <action>
RED — Write tests/unit/adapters/compression/test_file_redirect_determinism.py:
  - test_dict_ordering_stable_across_runs: feed the same input twice with Python hash randomization seeds differing (subprocess with PYTHONHASHSEED=0 vs PYTHONHASHSEED=123); both runs must produce byte-equal output
  - test_deterministic_flag_true: assert FileRedirectStrategy.deterministic is True
Commit RED as `test(10-01): RED — add FileRedirectStrategy determinism tests`. Confirm both fail.

GREEN — In file_redirect.py, sort any dict iteration used for output construction via `sorted(d.items())` (or equivalent). Flip the deterministic flag to True. Commit `feat(10-01): GREEN — stabilize FileRedirectStrategy dict ordering`.

REFACTOR — If a shared sort helper is warranted, extract; else skip. Re-run the Phase 9 adapter audit CLI (or existing audit test) to confirm FileRedirect now reports deterministic.
  </action>
  <verify>
pytest tests/unit/adapters/compression/test_file_redirect_determinism.py -v
pytest tests/unit/ -q
  </verify>
  <done>
FileRedirectStrategy produces byte-equal output across PYTHONHASHSEED variations; deterministic flag True; adapter audit clean for this strategy.
  </done>
</task>

<task type="auto">
  <name>Task 3: ProgressiveDisclosure timestamp determinism (RED→GREEN→REFACTOR)</name>
  <files>
    tests/unit/adapters/compression/test_progressive_disclosure_determinism.py
    src/token_sieve/adapters/compression/progressive_disclosure.py
  </files>
  <action>
RED — Write tests/unit/adapters/compression/test_progressive_disclosure_determinism.py:
  - test_timestamp_deterministic: construct strategy with a fixed clock (injected or monkeypatched time.time), assert two invocations at different real wall-clock moments produce byte-equal output
  - test_deterministic_flag_true
Commit RED. Confirm failures.

GREEN — Refactor ProgressiveDisclosureStrategy to accept a `clock: Callable[[], float] | None = None` parameter that defaults to time.time; use the injected clock everywhere timestamps are emitted. If the strategy emits timestamps that are not part of the cache-keyable output (e.g., only logging), remove them or gate behind a non-output channel. Flip deterministic flag. Commit `feat(10-01): GREEN — inject clock into ProgressiveDisclosure for determinism`.

REFACTOR — If other adapters use time.time for non-essential timestamps, note them in the Discovered Issues list (don't fix now — out of scope).
  </action>
  <verify>
pytest tests/unit/adapters/compression/test_progressive_disclosure_determinism.py -v
pytest tests/unit/ -q
  </verify>
  <done>
ProgressiveDisclosure output is wall-clock-independent; deterministic flag True.
  </done>
</task>

<task type="auto">
  <name>Task 4: compress.py threading behavioral regression guard (RED-as-contract)</name>
  <files>
    tests/regression/test_compress_py_threading_guard.py
  </files>
  <action>
This is an unusual task: there is NO production code change. The goal is to write a test file that pins down the CURRENT observable behavior of `_run_async` and `_run_async_bool` in src/token_sieve/cli/compress.py, so the wave 2 refactor has a contract to preserve.

Step 1 — Use jCodeMunch get_symbol_source on `_run_async`, `_run_async_bool`, and their immediate call sites in compress.py to understand exactly what they do (thread pool? asyncio.run? new loop per call? return type? exception propagation? are they idempotent?).

Step 2 — Write tests/regression/test_compress_py_threading_guard.py with class TestCompressPyThreadingGuard containing at minimum:
  - test_run_async_returns_coroutine_result: call _run_async with a trivial coroutine that returns a sentinel value; assert the sentinel is returned
  - test_run_async_propagates_exceptions: coroutine that raises ValueError; assert ValueError surfaces (not wrapped)
  - test_run_async_bool_returns_true_for_truthy: async callable returning True; assert True
  - test_run_async_bool_returns_false_for_falsy: async callable returning False; assert False
  - test_run_async_nested_invocation: call _run_async from inside another synchronous function that is itself called from a test body — exercises the "no running loop" path
  - test_run_async_does_not_leak_threads: snapshot threading.enumerate() before and after 10 invocations; assert no monotonic growth (within tolerance of 1 for daemon pools)
  - test_end_to_end_compress_call_uses_run_async: invoke a high-level compress.py entrypoint that internally calls _run_async; assert the end-to-end exit code and stdout are what the current tests already expect — this test must pass BEFORE the refactor and must still pass AFTER

These tests are GREEN from the first commit (they describe current behavior). Commit them with message `test(10-01): regression guard for compress.py threading (contract for wave 2 refactor)`.

If any test does NOT pass against current main, STOP — that's a pre-existing bug. Report it via the pre-existing issues pathway and either relax the assertion or mark xfail with a reference, then commit.
  </action>
  <verify>
pytest tests/regression/test_compress_py_threading_guard.py -v
pytest tests/unit/ -q
python -c "import token_sieve; print(token_sieve.__file__)"
  </verify>
  <done>
Regression guard file exists in tests/regression/; all tests green against current compress.py; test file is a behavioral contract that wave 2 must preserve.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wave merge hygiene (post-merge install + verify)</name>
  <files></files>
  <action>
After tasks 1-4 are merged from the worktree into main:
  1. cd to project root
  2. pip install -e .
  3. python -c "import token_sieve; print(token_sieve.__file__)" — assert path is in the working tree, not site-packages
  4. pytest -q — full suite green, 1627+ baseline preserved plus new tests
  5. Re-run the Phase 9 adapter determinism audit (whatever CLI or script exists) and confirm FileRedirect + ProgressiveDisclosure are now flagged deterministic
Commit any discovered environmental fixes as a separate `chore(10-01): post-merge hygiene` commit if needed.
  </action>
  <verify>
pytest -q
  </verify>
  <done>
main is green, editable install points to working tree, determinism audit clean for the two fixed strategies.
  </done>
</task>

</tasks>
<verification>
1. pytest -q — all baseline + new tests green
2. Adapter determinism audit shows FileRedirectStrategy and ProgressiveDisclosureStrategy as deterministic
3. tests/regression/test_compress_py_threading_guard.py is green and will act as contract for wave 2
4. Editable install verified: python -c "import token_sieve; print(token_sieve.__file__)" resolves to working tree
</verification>
<success_criteria>
- 4 hygiene items landed (2 DEVN-05 fixes, 2 determinism fixes)
- compress.py behavioral regression guard written against CURRENT implementation and green
- Adapter audit: 0 non-determinism findings for FileRedirect and ProgressiveDisclosure
- Full pytest suite green on main after merge
- Separate RED/GREEN commits for tasks 2 and 3; task 4 is a single GREEN-only contract-test commit (no production code)
- Post-merge editable install verified
</success_criteria>
<output>
10-01-SUMMARY.md
</output>
