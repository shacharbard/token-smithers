---
phase: 10
plan: "10-11"
title: "Intent budget modulation + cluster-tag secondary signal"
type: execute
wave: 11
depends_on: ["10-10"]
cross_phase_deps:
  - "phase-03: adapter budget config is read here"
autonomous: true
effort_override: balanced
skills_used:
  - python-pytest-patterns
files_modified:
  - src/token_sieve/intent/budget_modulation.py
  - src/token_sieve/intent/detector.py
  - src/token_sieve/domain/pipeline.py
  - src/token_sieve/config/defaults.py
  - tests/unit/intent/test_budget_modulation.py
  - tests/unit/intent/test_detector_cluster_secondary.py
  - tests/integration/intent/test_pipeline_modulation.py
forbidden_commands: []
must_haves:
  truths:
    - "New module src/token_sieve/intent/budget_modulation.py maps mode → multiplier: debugging=1.3, research=0.8, exploration=1.5, skimming=2.0, test_fix=1.4, refactor=1.2, unknown=1.0"
    - "Multipliers are config-overridable via intent.mode_multipliers dict"
    - "Multiplier applied as: effective_budget = max(1, int(base_budget * multiplier)) — never zero, always int"
    - "Cluster-tag secondary signal: when the current tool call touches a path whose cluster has already been visited, the detector biases toward 'debugging' mode (per CONTEXT §D3 planner guidance) EVEN if the n-gram pattern hasn't fired"
    - "The cluster bias is additive — it only kicks in when current_mode() would otherwise return 'unknown'; it never overrides a fired n-gram rule"
    - "Integration test: a full compress call with a seeded window+cluster-hit observes the modulated budget via a reranker or adapter-budget log/metric"
    - "Backward compatible: when intent.detection_enabled=False, effective_budget == base_budget byte-equal (legacy path)"
    - "RED commits separate from GREEN commits"
  artifacts:
    - {path: "src/token_sieve/intent/budget_modulation.py", provides: "Mode → budget multiplier mapping + apply()", contains: "def apply_modulation"}
    - {path: "tests/unit/intent/test_budget_modulation.py", provides: "Modulation contract tests"}
    - {path: "tests/integration/intent/test_pipeline_modulation.py", provides: "End-to-end pipeline budget modulation", contains: "test_modulated_budget_visible"}
---
<objective>
Close out GA3: wire the intent detector into the compression pipeline so detected modes translate into adapter budget multipliers. Add the cluster-tag secondary signal so "seen this cluster → bias debugging" works even when the n-gram pattern hasn't fired. This is the novel work per CONTEXT §D3 ("the novel work is the wiring, not the rules themselves").
</objective>
<context>
@.vbw-planning/phases/10-session-intelligence/10-CONTEXT.md (GA3 §D3, planner guidance)
@src/token_sieve/intent/detector.py (from wave 10)
@src/token_sieve/session/cluster_index.py (from wave 9)
@src/token_sieve/domain/pipeline.py (CompressionPipeline — integration point)
</context>
<tasks>

<task type="auto">
  <name>Task 1: budget_modulation module (RED→GREEN→REFACTOR)</name>
  <files>
    src/token_sieve/intent/budget_modulation.py
    tests/unit/intent/test_budget_modulation.py
  </files>
  <action>
RED — tests/unit/intent/test_budget_modulation.py:
  - test_debugging_multiplier_1_3: apply_modulation(mode="debugging", base=1000) == 1300
  - test_skimming_multiplier_2_0: apply_modulation("skimming", 1000) == 2000
  - test_research_multiplier_0_8: apply_modulation("research", 1000) == 800
  - test_unknown_multiplier_1_0: apply_modulation("unknown", 1000) == 1000
  - test_int_truncation: apply_modulation("debugging", 7) == int(7 * 1.3) = 9
  - test_minimum_budget_one: apply_modulation("research", 1) == max(1, int(1 * 0.8)) = 1
  - test_config_override: apply_modulation("debugging", 1000, multipliers={"debugging": 2.0}) == 2000
  - test_unknown_mode_uses_default_1_0: apply_modulation("bogus", 1000) == 1000
Commit RED.

GREEN — src/token_sieve/intent/budget_modulation.py:
  ```python
  DEFAULT_MULTIPLIERS = {
      "debugging": 1.3, "research": 0.8, "exploration": 1.5,
      "skimming": 2.0, "test_fix": 1.4, "refactor": 1.2, "unknown": 1.0,
  }
  def apply_modulation(mode: str, base_budget: int, multipliers: dict[str, float] | None = None) -> int:
      mul = (multipliers or DEFAULT_MULTIPLIERS).get(mode, 1.0)
      return max(1, int(base_budget * mul))
  ```
Commit GREEN.

REFACTOR — If multipliers need validation (e.g., reject negatives), add a config-time validator; else skip.
  </action>
  <verify>
pytest tests/unit/intent/test_budget_modulation.py -v
  </verify>
  <done>
Modulation table + apply function contract-tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cluster-tag secondary signal in detector (RED→GREEN)</name>
  <files>
    src/token_sieve/intent/detector.py
    tests/unit/intent/test_detector_cluster_secondary.py
  </files>
  <action>
RED — tests/unit/intent/test_detector_cluster_secondary.py:
  - test_unknown_mode_with_visited_cluster_biases_debugging: detector with no matching n-gram but current call touches a path in a visited cluster → current_mode() == "debugging"
  - test_fired_ngram_rule_not_overridden_by_cluster: window matches (Bash, Bash, Bash) → "exploration" even if the current call path is in a visited cluster
  - test_no_visited_cluster_no_bias: visited set is empty → current_mode returns "unknown" (legacy)
  - test_cluster_index_none_is_safe: detector constructed without ClusterIndex → cluster bias is a no-op
Commit RED.

GREEN — Extend IntentDetector:
  - Constructor optionally accepts cluster_index
  - observe(tool_name, file_path=None) — optional file_path arg
  - current_mode() returns n-gram match if any; else, if cluster_index is set AND the most-recent-observed path's cluster has been visited, returns "debugging"; else "unknown"
Commit GREEN.
  </action>
  <verify>
pytest tests/unit/intent/test_detector_cluster_secondary.py -v
pytest tests/unit/intent/ -q
  </verify>
  <done>
Cluster-tag secondary signal biases toward debugging only when no n-gram fires; legacy path unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Pipeline integration — effective budget modulation (RED→GREEN→REFACTOR)</name>
  <files>
    src/token_sieve/domain/pipeline.py
    tests/integration/intent/test_pipeline_modulation.py
  </files>
  <action>
RED — tests/integration/intent/test_pipeline_modulation.py:
  - test_modulated_budget_visible: configure a CompressionPipeline with a detector observing (Read,Read,Read,Read); run a compress call with a large input; assert the adapter received an effective_budget == base_budget * 2.0 (skimming)
  - test_legacy_path_byte_equal: configure pipeline with intent.detection_enabled=False; run two calls with identical input; assert output is byte-equal to the pre-wave-11 baseline (capture via golden fixture or hash)
  - test_research_compresses_less: mode="research" produces a smaller budget → more aggressive compression than "unknown"
  - test_detector_observes_every_tool_call: a sequence of 10 tool calls results in 10 observe() invocations
Commit RED.

GREEN — In src/token_sieve/domain/pipeline.py (use jCodeMunch to find the CompressionPipeline class and its budget-read path):
  - Accept an optional `intent_detector: IntentDetector | None` via constructor
  - On each compress() call: detector.observe(tool_name, file_path); mode = detector.current_mode(); effective_budget = apply_modulation(mode, base_budget, config.intent.mode_multipliers)
  - Pass effective_budget down to the adapter invocation instead of base_budget
  - When intent_detector is None or intent.detection_enabled=False, use base_budget unchanged (legacy byte-equal path)
Commit GREEN.

REFACTOR — If the budget-read path is scattered across adapters, introduce a small BudgetContext object that carries both base and effective; only factor this out if the change is cleanly additive.
  </action>
  <verify>
pytest tests/integration/intent/ tests/unit/intent/ -v
pytest -q
  </verify>
  <done>
Pipeline applies mode → multiplier; legacy path byte-equal when flag off; full intent loop live.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave merge + hygiene</name>
  <files></files>
  <action>
pip install -e . && pytest -q. Smoke test: a seeded 4-Read sequence through compress visibly emits a skimming-multiplier budget in logs/metrics.
  </action>
  <verify>
pytest -q
  </verify>
  <done>
main green; intent loop fully wired; success criteria for GA3 met.
  </done>
</task>

</tasks>
<verification>
1. pytest tests/unit/intent/ tests/integration/intent/ -v
2. pytest -q — baseline green
3. Legacy byte-equal path verified via golden fixture
</verification>
<success_criteria>
- budget_modulation.apply_modulation ships with 6 mode multipliers + default fallback
- Cluster-tag secondary signal: visited cluster biases to "debugging" only when no n-gram fires
- CompressionPipeline reads effective_budget = base * multiplier
- intent.detection_enabled=False preserves legacy byte-equal path
- Full GA3 loop live: observation → detection → modulation → adapter budget
- RED commits separate from GREEN
</success_criteria>
<output>
10-11-SUMMARY.md
</output>
