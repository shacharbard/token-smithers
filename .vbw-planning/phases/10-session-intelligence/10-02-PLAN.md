---
phase: 10
plan: "10-02"
title: "compress.py threading refactor (quarantined)"
type: execute
wave: 2
depends_on: ["10-01"]
cross_phase_deps:
  - "phase-09: all compress.py end-to-end tests must stay green byte-equal"
autonomous: true
effort_override: max
skills_used:
  - python-pytest-patterns
files_modified:
  - src/token_sieve/cli/compress.py
forbidden_commands: []
must_haves:
  truths:
    - "NO other feature work lands in this wave — compress.py is load-bearing for every compressed tool call"
    - "tests/regression/test_compress_py_threading_guard.py (from wave 1) stays green through every commit of this wave"
    - "_run_async and _run_async_bool are removed or reduced to thin shims; sync/async duplication eliminated per project_phase10_backlog.md"
    - "All Phase 9 e2e tests, all CLI tests, and the full 1627+ baseline stay green"
    - "Refactor preserves exit-code propagation, stderr passthrough, fail-open annotation behavior, ring-buffer wiring — all byte-equal to pre-refactor"
    - "Every commit in this wave is either a pure refactor (tests unchanged) OR a test+code pair; no production code commit lands without a test that covers it"
    - "Post-merge hygiene: pip install -e ., import path check, full pytest green before wave 3 begins"
  artifacts:
    - {path: "src/token_sieve/cli/compress.py", provides: "Refactored CLI compressor with simplified async boundary", contains: "def run"}
  key_links:
    - {from: "tests/regression/test_compress_py_threading_guard.py", to: "src/token_sieve/cli/compress.py", via: "contract test from wave 1 gates every refactor commit"}
---
<objective>
Quarantined wave 2. Remove the `_run_async` / `_run_async_bool` threading workarounds from compress.py and collapse the sync/async duplication. This is the highest-risk wave in Phase 10 — it touches the hot path for every compressed tool call. Success criterion: the wave 1 regression guard stays green from the first refactor commit through the last, and no observable behavior changes.
</objective>
<context>
@.vbw-planning/phases/10-session-intelligence/10-CONTEXT.md
@tests/regression/test_compress_py_threading_guard.py
Source to refactor (use jCodeMunch get_symbol_source, not raw Read):
- src/token_sieve/cli/compress.py — full module including `run`, `_run_async`, `_run_async_bool`, any helpers
Related memory: feedback_tdd_enforcement.md, project_phase10_backlog.md (compress.py simplification + sync/async removal item), project_phase09_gotchas.md (async+SQLite minefield, locale scope — any refactor must keep the LC_ALL=C try/finally block intact)
</context>
<tasks>

<task type="auto">
  <name>Task 1: Characterize current async boundary + write finer-grained RED tests</name>
  <files>
    tests/unit/cli/test_compress_refactor.py
  </files>
  <action>
Read _run_async and _run_async_bool via jCodeMunch get_symbol_source. Identify the architectural smell: is it nested-event-loop avoidance? asyncio.run-in-thread? Document the specific cleanup intent in the test file's module docstring.

Write tests/unit/cli/test_compress_refactor.py with unit-level tests that describe the DESIRED post-refactor behavior shape:
  - test_compress_run_is_fully_synchronous: compress.run([...]) is a plain sync function; no `async def` in the public API
  - test_no_private_run_async_helpers_after_refactor: `assert not hasattr(compress, "_run_async")` AND `assert not hasattr(compress, "_run_async_bool")` — OR, if the helpers remain as thin shims, they must delegate to a single async entrypoint
  - test_single_async_boundary: use mocker/patch to count how many times asyncio.run (or equivalent) is invoked per compress.run call — assert exactly 0 or 1, not 2+
These tests MUST fail against current main (RED). Commit as `test(10-02): RED — pin compress.py post-refactor shape`.
  </action>
  <verify>
pytest tests/unit/cli/test_compress_refactor.py -v  # expect failures
pytest tests/regression/test_compress_py_threading_guard.py -v  # expect green
  </verify>
  <done>
New RED tests describe the refactored shape; wave 1 regression guard still green; commit lands RED-only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Collapse _run_async into a single async boundary (GREEN)</name>
  <files>
    src/token_sieve/cli/compress.py
  </files>
  <action>
GREEN — Refactor compress.py:
  - Identify the single place where an async call must cross into sync (most likely the ring-buffer or learning-store write if those are async)
  - Replace scattered _run_async / _run_async_bool calls with either (a) fully synchronous equivalents (if the callees have sync APIs) or (b) a single asyncio.run at the outermost boundary
  - Preserve: exit code propagation, TSIEV_WRAP_CMD pop, stderr passthrough + cargo/docker/webpack override, fail-open annotation, RingBuffer wiring, LC_ALL=C try/finally scope (from Phase 9 wave 5)
  - Delete _run_async / _run_async_bool if fully unused; otherwise leave as a single documented thin shim

Run in this order after each small change:
  1. pytest tests/regression/test_compress_py_threading_guard.py -v  (must stay green)
  2. pytest tests/unit/cli/test_compress_refactor.py -v  (should progress toward green)
  3. pytest tests/unit/cli/test_compress.py -v  (Phase 9 e2e)
  4. pytest tests/unit/ -q  (full baseline)

Commit in small refactor slices: each commit must keep ALL of the above green. Typical slices:
  - chore(10-02): inline _run_async at call site 1
  - chore(10-02): inline _run_async at call site 2
  - refactor(10-02): collapse _run_async_bool into _run_async
  - refactor(10-02): remove now-unused _run_async helper

Final commit once all new tests pass: `feat(10-02): GREEN — single async boundary in compress.py`.
  </action>
  <verify>
pytest tests/regression/test_compress_py_threading_guard.py tests/unit/cli/test_compress_refactor.py tests/unit/cli/test_compress.py -v
pytest tests/unit/ -q
  </verify>
  <done>
All compress.py tests green; wave 1 regression guard unbroken; either _run_async helpers removed or reduced to documented thin shims; single asyncio boundary observable in a single compress.run call.
  </done>
</task>

<task type="auto">
  <name>Task 3: sync/async duplication removal</name>
  <files>
    src/token_sieve/cli/compress.py
  </files>
  <action>
Per project_phase10_backlog.md "sync/async removal" item: identify any remaining pairs of functions in compress.py that exist in both sync and async flavors. Pick one flavor (sync preferred since we now have a single async boundary). Delete the other. Update call sites.

If the duplication reveals that the async flavor was load-bearing for a downstream caller outside compress.py, STOP and report — cross-module cleanup is out of scope for this wave. Only inner-module duplication is in scope here.

Commit as `refactor(10-02): remove sync/async duplication in compress.py`. Regression guard must stay green.
  </action>
  <verify>
pytest tests/regression/test_compress_py_threading_guard.py tests/unit/cli/ -v
pytest tests/unit/ -q
  </verify>
  <done>
compress.py contains one implementation path per behavior; no sync-wrapping-async-wrapping-sync chains remain.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave merge + post-merge hygiene</name>
  <files></files>
  <action>
After tasks 1-3 merge to main:
  1. pip install -e . from project root
  2. python -c "import token_sieve; print(token_sieve.__file__)" — verify working tree
  3. pytest -q — full suite green including the regression guard
  4. Smoke test: `TSIEV_WRAP_CMD="echo hello" python -m token_sieve.cli compress --wrap-env; echo $?` prints "hello\n0"
  5. Smoke test exit code: `TSIEV_WRAP_CMD="bash -c 'exit 7'" python -m token_sieve.cli compress --wrap-env; echo $?` prints 7
  6. Smoke test recall: prior compress invocation is retrievable via `python -m token_sieve.cli recall`
  </action>
  <verify>
pytest -q
  </verify>
  <done>
main is green, compress.py refactor landed without observable behavior drift, wave 1 regression guard + Phase 9 e2e tests + full baseline all green.
  </done>
</task>

</tasks>
<verification>
1. pytest tests/regression/test_compress_py_threading_guard.py -v — green (wave 1 contract preserved)
2. pytest tests/unit/cli/test_compress.py tests/unit/cli/test_compress_refactor.py -v — green (Phase 9 + refactor shape)
3. pytest -q — full 1627+ baseline green
4. git log --oneline on this wave's branch — small refactor slices, each self-contained and green
5. Manual smoke tests on compress entrypoint pass
</verification>
<success_criteria>
- _run_async / _run_async_bool removed or reduced to thin documented shims
- Single async boundary in compress.py
- sync/async duplication eliminated
- wave 1 regression guard green through every refactor commit
- Phase 9 end-to-end compress tests byte-equal
- Full baseline green
- No feature work in this wave — compress.py only
</success_criteria>
<output>
10-02-SUMMARY.md
</output>
