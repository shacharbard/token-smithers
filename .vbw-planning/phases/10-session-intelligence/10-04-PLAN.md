---
phase: 10
plan: "10-04"
title: "Parameter-aware cache keys (non-file-scoped tools)"
type: execute
wave: 4
depends_on: ["10-03"]
cross_phase_deps:
  - "phase-03: extends cache key computation; pays off project_cache_design_debt fuzzy-cache-disabled issue"
autonomous: true
effort_override: balanced
skills_used:
  - python-pytest-patterns
files_modified:
  - src/token_sieve/adapters/cache/param_normalizer.py
  - src/token_sieve/adapters/cache/call_cache.py
  - tests/unit/adapters/cache/test_param_normalizer.py
  - tests/unit/adapters/cache/test_call_cache_param_keys.py
  - src/token_sieve/config/defaults.py
forbidden_commands: []
must_haves:
  truths:
    - "Cache keys for non-file-scoped tools (WebFetch, stats queries, search) include normalized parameter dict, not just tool name"
    - "Normalization is deterministic: sort keys, strip whitespace from string values, canonicalize URL parameters (sorted query params, lowercased scheme/host)"
    - "Two calls with semantically equivalent parameters (e.g., {a:1, b:2} vs {b:2, a:1}) produce identical cache keys"
    - "Two calls with semantically different parameters produce different cache keys — no fuzzy matching"
    - "Feature flag `cache.fuzzy_matching` remains OFF; new flag `cache.parameter_aware_keys` defaults to ON"
    - "Pays off project_cache_design_debt.md 30% false-hit-rate by enforcing exact-key matching for non-file-scoped tools"
    - "RED commits separate from GREEN commits"
  artifacts:
    - {path: "src/token_sieve/adapters/cache/param_normalizer.py", provides: "Parameter normalization + canonical key derivation", contains: "def normalize_params"}
    - {path: "tests/unit/adapters/cache/test_param_normalizer.py", provides: "Normalization contract tests", contains: "class TestNormalizeParams"}
  key_links:
    - {from: "src/token_sieve/adapters/cache/call_cache.py", to: "src/token_sieve/adapters/cache/param_normalizer.py", via: "cache key builder calls normalize_params before hashing"}
---
<objective>
Orthogonal baseline for cache coherence: engineer exact, parameter-aware cache keys for tools whose responses cannot be file-scoped (WebFetch, stats queries, search). This pays off project_cache_design_debt directly by replacing the disabled fuzzy-matching approach with deterministic key construction. Runs in its own wave after Tier 1 so that the cache key code path is touched only once per wave.
</objective>
<context>
@.vbw-planning/phases/10-session-intelligence/10-CONTEXT.md (GA2 §Parameter-aware keys)
@src/token_sieve/adapters/cache/param_normalizer.py (file already exists per project inventory — extend, do not recreate)
Existing code (jCodeMunch):
- src/token_sieve/adapters/cache/param_normalizer.py — current state; extend
- src/token_sieve/adapters/cache/call_cache.py — cache key function
Memory: project_cache_design_debt.md (30% false-hit on fuzzy).
</context>
<tasks>

<task type="auto">
  <name>Task 1: Survey + extend normalize_params (RED→GREEN→REFACTOR)</name>
  <files>
    src/token_sieve/adapters/cache/param_normalizer.py
    tests/unit/adapters/cache/test_param_normalizer.py
  </files>
  <action>
Use jCodeMunch get_symbol_source on current param_normalizer.py to see what's already there. Do NOT duplicate existing functionality; extend.

RED — tests/unit/adapters/cache/test_param_normalizer.py (add or extend):
  - test_dict_key_order_irrelevant: {a:1,b:2} and {b:2,a:1} produce the same canonical form
  - test_nested_dicts_sorted: {x:{c:3,a:1}} canonical form has sorted inner keys
  - test_string_whitespace_stripped: {"q":"  foo bar  "} → {"q":"foo bar"}
  - test_url_query_params_sorted: normalize_url("https://x.com/a?b=2&a=1") == normalize_url("https://x.com/a?a=1&b=2")
  - test_url_scheme_host_lowercased: "HTTPS://X.COM/a" → "https://x.com/a"
  - test_url_fragment_dropped: "https://x.com/a#frag" normalizes to "https://x.com/a" (fragments don't hit the server)
  - test_none_values_preserved_not_dropped: {"q":None} stays distinct from {}
  - test_list_order_preserved: lists are positional, not sets (["a","b"] != ["b","a"])
Commit RED.

GREEN — Extend normalize_params with a canonical form builder. If URL normalization doesn't already exist, add `normalize_url(s: str) → str` using urllib.parse with sorted query params via parse_qsl + urlencode. Commit GREEN.

REFACTOR — If the normalized form is passed into json.dumps elsewhere, make sure sort_keys=True is used consistently.
  </action>
  <verify>
pytest tests/unit/adapters/cache/test_param_normalizer.py -v
pytest tests/unit/ -q
  </verify>
  <done>
normalize_params handles dict ordering, nesting, whitespace, URL canonicalization, and edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cache key builder uses normalized params (RED→GREEN)</name>
  <files>
    src/token_sieve/adapters/cache/call_cache.py
    tests/unit/adapters/cache/test_call_cache_param_keys.py
  </files>
  <action>
RED — tests/unit/adapters/cache/test_call_cache_param_keys.py:
  - test_equivalent_params_hit_same_key: cache.put(tool="WebFetch", params={"url":"https://x.com/?a=1&b=2"}, value="V"); cache.get(tool="WebFetch", params={"url":"https://x.com/?b=2&a=1"}) → returns "V"
  - test_different_params_miss: ditto with {"url":"https://y.com"} → returns None
  - test_parameter_aware_feature_flag_off_falls_back: with cache.parameter_aware_keys=False, the equivalent-params test MISSES (exact string match), verifying the flag actually controls behavior
  - test_file_scoped_and_param_aware_interact: entry with both a manifest AND param-aware key — both checks run; either mismatch → miss
Commit RED.

GREEN — In call_cache.py key construction, if the feature flag is on and the tool is in the non-file-scoped list (or all tools — simpler default), run normalize_params on the params dict before hashing. Flag default ON. Commit GREEN.
  </action>
  <verify>
pytest tests/unit/adapters/cache/test_call_cache_param_keys.py -v
pytest tests/unit/ -q
  </verify>
  <done>
Parameter-aware keys hit/miss as expected; feature flag controls the behavior; interacts correctly with Tier 1 manifest.
  </done>
</task>

<task type="auto">
  <name>Task 3: Config flag + documentation (RED→GREEN)</name>
  <files>
    src/token_sieve/config/defaults.py
    tests/unit/config/test_defaults_cache_flags.py
  </files>
  <action>
RED — test_defaults: assert `cache.parameter_aware_keys` default is True, `cache.fuzzy_matching` remains False.
Commit RED.

GREEN — Add the flag to config/defaults.py. If there's a config schema/validation file, extend it. Commit GREEN.
  </action>
  <verify>
pytest tests/unit/config/ -v
pytest tests/unit/ -q
  </verify>
  <done>
New flag present, fuzzy stays off.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wave merge + hygiene</name>
  <files></files>
  <action>
pip install -e . && python -c "import token_sieve; print(token_sieve.__file__)" && pytest -q
  </action>
  <verify>
pytest -q
  </verify>
  <done>
Baseline green, new flag default on, Tier 1 + param-aware keys interact cleanly.
  </done>
</task>

</tasks>
<verification>
1. pytest tests/unit/adapters/cache/ -v — param tests + Tier 1 tests all green
2. Feature flag default ON, fuzzy OFF
3. pytest -q — baseline green
</verification>
<success_criteria>
- normalize_params handles dict ordering, URL canonicalization, whitespace, edge cases
- Cache lookups for equivalent parameters return cache hits
- Cache lookups for different parameters return cache misses (no fuzzy matching)
- Feature flag wired with safe defaults
- Interacts cleanly with Tier 1 manifest path
- Project cache_design_debt 30% false-hit-rate issue demonstrably addressed by the param tests
</success_criteria>
<output>
10-04-SUMMARY.md
</output>
