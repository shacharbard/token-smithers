---
phase: 1
plan: 1
title: "Project Scaffolding, Domain Model & Ports"
wave: 1
depends_on: []
skills_used:
  - pydantic
  - python-pytest-patterns
must_haves:
  truths:
    - "src/token_sieve/domain/ imports nothing outside stdlib"
    - "All value objects are @dataclass(frozen=True)"
    - "All Protocol classes use typing.Protocol, no ABC"
    - "ContentEnvelope uses MappingProxyType for metadata immutability"
  artifacts:
    - path: "pyproject.toml"
      contains: ["token-sieve", "pytest", "cov-fail-under=100"]
    - path: "src/token_sieve/domain/model.py"
      contains: ["ContentEnvelope", "ContentType", "CompressionEvent", "TokenBudget", "CompressedResult"]
    - path: "src/token_sieve/domain/ports.py"
      contains: ["CompressionStrategy", "DeduplicationStrategy", "BackendToolAdapter", "SessionRepository", "MetricsCollector", "TokenCounter"]
    - path: "tests/unit/domain/test_model.py"
      contains: ["test_content_envelope", "test_compression_event", "test_token_budget"]
    - path: "tests/unit/domain/test_ports.py"
      contains: ["CompressionStrategyContract", "test_protocol_structural_subtyping"]
  key_links:
    - from: "ports.py"
      to: "model.py"
      relation: "Protocols reference ContentEnvelope, ContentType, CompressionEvent"
---

## Objective

Create the project skeleton (pyproject.toml, src-layout, test directories) and implement all domain value objects (ContentEnvelope, CompressionEvent, TokenBudget, CompressedResult, ContentType enum) plus all Protocol interfaces (CompressionStrategy, DeduplicationStrategy, BackendToolAdapter, SessionRepository, MetricsCollector, TokenCounter). This plan establishes the types that every other plan depends on.

## Context

- @.vbw-planning/phases/01-domain-core/01-01-RESEARCH.md — frozen dataclass patterns, Protocol best practices, project structure, contract test pattern
- @.vbw-planning/phases/01-domain-core/01-CONTEXT.md — ContentEnvelope decision, dual-mode token counting, Repository Protocol, CompressionEvent, MetricsCollector
- @.vbw-planning/REQUIREMENTS.md — REQ-01 through REQ-06, REQ-11, REQ-17
- @SKILL:pydantic — value object validation patterns (reference only; domain uses stdlib dataclasses, not pydantic)
- @SKILL:python-pytest-patterns — contract test base class, factory fixtures, parametrize, conftest structure

## Tasks

### Task 1: Project scaffolding and pyproject.toml (TDD: RED for import test)
**Files:** `pyproject.toml`, `src/token_sieve/__init__.py`, `src/token_sieve/domain/__init__.py`, `tests/__init__.py`, `tests/conftest.py`, `tests/unit/__init__.py`, `tests/unit/domain/__init__.py`, `tests/unit/domain/conftest.py`

1. Create `pyproject.toml` with src-layout, pytest config (`--cov=token_sieve.domain --cov-fail-under=100 --cov-branch`), and zero external dependencies for the core package
2. Create all `__init__.py` files for the package hierarchy
3. Create `tests/conftest.py` with session-scoped shared fixtures placeholder
4. Create `tests/unit/domain/conftest.py` with factory fixtures (`make_envelope`, `make_event`)
5. RED: Write `tests/unit/domain/test_model.py` with a basic import test that imports `ContentEnvelope` from `token_sieve.domain.model` -- must fail (module does not exist yet)

### Task 2: Domain model value objects (TDD: RED then GREEN)
**Files:** `src/token_sieve/domain/model.py`, `tests/unit/domain/test_model.py`
**Reqs:** REQ-03, REQ-04, REQ-17

1. RED: Write comprehensive tests for all value objects in `test_model.py`:
   - `ContentType` enum: TEXT, JSON, CODE, CLI_OUTPUT, UNKNOWN
   - `ContentEnvelope`: construction, frozen immutability, metadata as MappingProxyType, validation (empty content raises ValueError), `dataclasses.replace()` transformation
   - `CompressionEvent`: construction, `savings_ratio` property, zero-token edge case
   - `TokenBudget`: construction, `remaining` calculation, `is_exceeded` check, `consume()` returns new instance
   - `CompressedResult`: construction with envelope + events list
2. GREEN: Implement `model.py` with all value objects as `@dataclass(frozen=True)`. Use `types.MappingProxyType` for ContentEnvelope metadata. Implement `__post_init__` validation.
3. Confirm all tests pass.

### Task 3: Domain Protocol interfaces (TDD: RED then GREEN)
**Files:** `src/token_sieve/domain/ports.py`, `tests/unit/domain/test_ports.py`
**Reqs:** REQ-01, REQ-02, REQ-05, REQ-06, REQ-09, REQ-11, REQ-17

1. RED: Write tests in `test_ports.py`:
   - Structural subtyping smoke tests: a plain class implementing the right methods satisfies each Protocol (use `isinstance` for `@runtime_checkable` ones, type-check assertions for others)
   - `CompressionStrategyContract` base class with `test_can_handle_returns_bool`, `test_compress_returns_envelope`, `test_compress_preserves_content_type`
   - Import-only smoke test for all Protocols
2. GREEN: Implement `ports.py` with all Protocol classes:
   - `CompressionStrategy`: `can_handle(envelope) -> bool`, `compress(envelope) -> ContentEnvelope`
   - `DeduplicationStrategy`: `is_duplicate(envelope, session) -> bool`, `get_reference(envelope, session) -> str`
   - `BackendToolAdapter`: `call_tool(name, arguments) -> ToolResult`
   - `SessionRepository`: `get(session_id) -> SessionContext | None`, `save(session) -> None`
   - `MetricsCollector`: `record(event) -> None`, `session_summary() -> dict`, `strategy_breakdown() -> dict`
   - `TokenCounter`: `count(text) -> int` (mark `@runtime_checkable`)
3. Confirm all tests pass.

### Task 4: Contract test base classes and factory fixtures (REFACTOR)
**Files:** `tests/unit/domain/conftest.py`, `tests/unit/domain/test_ports.py`

1. Flesh out `conftest.py` factory fixtures: `make_envelope(content, content_type, metadata)`, `make_event(original_tokens, compressed_tokens, strategy_name)`, `make_budget(total, used)`
2. Add `MockStrategy` fixture to conftest that satisfies `CompressionStrategyContract`
3. Create `TestMockStrategy(CompressionStrategyContract)` in test_ports.py to validate the contract test pattern works end-to-end
4. REFACTOR: ensure all test_model.py tests use factory fixtures from conftest where appropriate
5. Run full test suite, confirm 100% coverage on model.py and ports.py

## Verification

```bash
cd /Users/shaharbard/Development/AI/token-sieve
python -m pytest tests/unit/domain/test_model.py tests/unit/domain/test_ports.py -v --tb=short
python -c "from token_sieve.domain.model import ContentEnvelope, ContentType, CompressionEvent, TokenBudget, CompressedResult; print('All model imports OK')"
python -c "from token_sieve.domain.ports import CompressionStrategy, DeduplicationStrategy, BackendToolAdapter, SessionRepository, MetricsCollector, TokenCounter; print('All port imports OK')"
python -m pytest tests/unit/domain/ --cov=token_sieve.domain.model --cov=token_sieve.domain.ports --cov-fail-under=100 --cov-branch -q
```

## Success Criteria

- `pyproject.toml` exists with correct src-layout and pytest config
- All 5 value objects implemented as frozen dataclasses with validation
- All 6 Protocol interfaces defined in ports.py
- `CompressionStrategyContract` base class validates mock implementation
- Factory fixtures available in conftest for downstream plans
- 100% branch coverage on model.py and ports.py
- Zero external dependencies (only stdlib imports in domain/)
