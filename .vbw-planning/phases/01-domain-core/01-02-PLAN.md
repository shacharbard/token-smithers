---
phase: 1
plan: 2
title: "Domain Services: Pipeline, Session, Counters, Metrics"
wave: 2
depends_on: [1]
skills_used:
  - python-pytest-patterns
must_haves:
  truths:
    - "CompressionPipeline.process() returns (ContentEnvelope, list[CompressionEvent]) tuple"
    - "Pipeline uses dict[ContentType, list[CompressionStrategy]] routing"
    - "CharEstimateCounter uses chars/4 approximation"
    - "InMemorySessionRepo stores sessions in a plain dict"
    - "All service files import only from domain.model and domain.ports"
  artifacts:
    - path: "src/token_sieve/domain/pipeline.py"
      contains: ["CompressionPipeline", "register", "process"]
    - path: "src/token_sieve/domain/session.py"
      contains: ["InMemorySessionRepo", "SessionContext"]
    - path: "src/token_sieve/domain/counters.py"
      contains: ["CharEstimateCounter", "count"]
    - path: "src/token_sieve/domain/metrics.py"
      contains: ["InMemoryMetricsCollector", "record", "session_summary", "strategy_breakdown"]
    - path: "tests/unit/domain/test_pipeline.py"
      contains: ["test_process_routes_by_content_type", "test_process_chains_strategies", "test_process_returns_events"]
  key_links:
    - from: "pipeline.py"
      to: "ports.py"
      relation: "Pipeline accepts CompressionStrategy instances, uses TokenCounter"
    - from: "session.py"
      to: "ports.py"
      relation: "InMemorySessionRepo implements SessionRepository Protocol"
    - from: "metrics.py"
      to: "ports.py"
      relation: "InMemoryMetricsCollector implements MetricsCollector Protocol"
---

## Objective

Implement the four domain service modules: CompressionPipeline (content-routed strategy chain), InMemorySessionRepo, CharEstimateCounter, and InMemoryMetricsCollector. These are the behavioral core of the domain -- they wire together the Protocols and value objects from Plan 1 into working logic.

## Context

- @.vbw-planning/phases/01-domain-core/01-01-RESEARCH.md — content-routed pipeline pattern (dict router + chain-of-responsibility), contract test pattern, process() returning tuple
- @.vbw-planning/phases/01-domain-core/01-CONTEXT.md — dual-mode token counting, InMemorySessionRepo, MetricsCollector Protocol, CompressionEvent observability
- @.vbw-planning/REQUIREMENTS.md — REQ-01, REQ-02, REQ-03, REQ-05, REQ-06, REQ-17
- @SKILL:python-pytest-patterns — parametrize for pipeline routing tests, mock strategies, fixture scoping

## Tasks

### Task 1: CompressionPipeline service (TDD: RED then GREEN)
**Files:** `src/token_sieve/domain/pipeline.py`, `tests/unit/domain/test_pipeline.py`
**Reqs:** REQ-01, REQ-02

1. RED: Write `test_pipeline.py` with comprehensive tests:
   - `test_process_empty_pipeline_returns_input_unchanged` — no registered strategies, envelope passes through
   - `test_process_routes_by_content_type` — TEXT strategies only run for TEXT envelopes, not JSON
   - `test_process_chains_strategies_in_order` — two strategies registered for same type, second receives output of first
   - `test_process_returns_events` — each strategy that fires produces a CompressionEvent in the result tuple
   - `test_process_skips_strategy_when_can_handle_false` — strategy with `can_handle=False` is skipped
   - `test_register_adds_strategy_to_route` — verify registration builds the routes dict
   - `test_process_with_token_counter` — pipeline uses injected TokenCounter for event token counts
   - Use mock strategies (simple classes implementing compress/can_handle)
2. GREEN: Implement `pipeline.py`:
   - `@dataclass` class `CompressionPipeline` with `_routes: dict[ContentType, list[CompressionStrategy]]` and `_counter: TokenCounter`
   - `register(content_type, strategy)` method
   - `process(envelope) -> tuple[ContentEnvelope, list[CompressionEvent]]` — iterates chain, collects events, returns tuple
3. Confirm all pipeline tests pass.

### Task 2: CharEstimateCounter (TDD: RED then GREEN)
**Files:** `src/token_sieve/domain/counters.py`, `tests/unit/domain/test_counters.py`
**Reqs:** REQ-03

1. RED: Write `test_counters.py`:
   - `test_char_estimate_empty_string` — returns 0
   - `test_char_estimate_known_text` — "hello world" (11 chars) returns 3 (11//4)
   - `test_char_estimate_satisfies_token_counter_protocol` — isinstance check (runtime_checkable)
   - Parametrize with various input lengths to validate chars/4 formula
2. GREEN: Implement `counters.py` with `CharEstimateCounter` class implementing `count(text) -> int` as `max(1, len(text) // 4)` (minimum 1 for non-empty text, 0 for empty).
3. Confirm tests pass.

### Task 3: InMemorySessionRepo and SessionContext (TDD: RED then GREEN)
**Files:** `src/token_sieve/domain/session.py`, `tests/unit/domain/test_session.py`
**Reqs:** REQ-04, REQ-06

1. RED: Write `test_session.py`:
   - `test_get_nonexistent_returns_none`
   - `test_save_and_get_roundtrip`
   - `test_save_overwrites_existing`
   - `test_session_context_tracks_seen_hashes` — SessionContext entity with hash tracking for dedup
   - `test_session_context_add_result_hash`
   - `test_satisfies_session_repository_protocol` — structural subtyping check
2. GREEN: Implement `session.py`:
   - `SessionContext` entity (mutable, not frozen): `session_id: str`, `seen_hashes: set[str]`, `result_count: int`, `created_at: datetime`
   - `InMemorySessionRepo`: `_store: dict[str, SessionContext]`, implements `get()` and `save()`
3. Confirm tests pass.

### Task 4: InMemoryMetricsCollector (TDD: RED then GREEN)
**Files:** `src/token_sieve/domain/metrics.py`, `tests/unit/domain/test_metrics.py`
**Reqs:** REQ-17

1. RED: Write `test_metrics.py`:
   - `test_record_stores_event`
   - `test_session_summary_returns_totals` — total original tokens, total compressed tokens, overall savings ratio
   - `test_strategy_breakdown_groups_by_strategy` — per-strategy counts and savings
   - `test_empty_collector_returns_zero_summary`
   - `test_satisfies_metrics_collector_protocol`
2. GREEN: Implement `metrics.py` with `InMemoryMetricsCollector`:
   - `_events: list[CompressionEvent]`
   - `record(event)` appends to list
   - `session_summary()` returns dict with totals
   - `strategy_breakdown()` returns dict keyed by strategy_name
3. Confirm tests pass.

### Task 5: Integration wiring and coverage (REFACTOR)
**Files:** `src/token_sieve/domain/__init__.py`, `tests/unit/domain/test_pipeline.py`

1. Update `domain/__init__.py` to export key public API: ContentEnvelope, ContentType, CompressionPipeline, CompressionEvent, TokenBudget, CompressedResult, CharEstimateCounter
2. Add pipeline integration test: create pipeline with CharEstimateCounter, register a mock strategy, process an envelope, verify events have correct token counts from the real counter
3. REFACTOR: review all 4 service modules for consistency -- naming, docstrings, import style
4. Run full domain test suite with coverage: `pytest tests/unit/domain/ --cov=token_sieve.domain --cov-fail-under=100 --cov-branch`
5. Fix any coverage gaps (likely branch coverage in edge cases)

## Verification

```bash
cd /Users/shaharbard/Development/AI/token-sieve
python -m pytest tests/unit/domain/ -v --tb=short
python -c "from token_sieve.domain.pipeline import CompressionPipeline; print('Pipeline import OK')"
python -c "from token_sieve.domain.counters import CharEstimateCounter; c = CharEstimateCounter(); print(f'Token count: {c.count(\"hello world\")}')"
python -c "from token_sieve.domain.session import InMemorySessionRepo, SessionContext; print('Session import OK')"
python -c "from token_sieve.domain.metrics import InMemoryMetricsCollector; print('Metrics import OK')"
python -m pytest tests/unit/domain/ --cov=token_sieve.domain --cov-fail-under=100 --cov-branch -q
```

## Success Criteria

- CompressionPipeline routes envelopes by ContentType and chains strategies correctly
- Pipeline.process() returns `(ContentEnvelope, list[CompressionEvent])` tuple
- CharEstimateCounter passes TokenCounter Protocol check
- InMemorySessionRepo get/save roundtrip works
- InMemoryMetricsCollector tracks events and produces summaries
- All domain services use only stdlib imports
- 100% branch coverage across all domain modules
